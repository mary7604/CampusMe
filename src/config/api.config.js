// ─── Source unique de vérité pour l'URL du backend ──────────────
// Remplace l'IP par celle de ton PC (ipconfig dans le terminal)

const API_CONFIG = {
  BASE_URL: 'http://192.168.1.103:3000',
  TIMEOUT: 10000,
};

export default API_CONFIG;
